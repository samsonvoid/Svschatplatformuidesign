
  # SVS Chat Platform UI Design

  This is a code bundle for SVS Chat Platform UI Design. The original project is available at https://www.figma.com/design/qa4TUcpyammpxdki24EAdI/SVS-Chat-Platform-UI-Design.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  